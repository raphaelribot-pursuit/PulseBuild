/**
 * Seed data constants.
 * Source: SoT v2.0 Section 7 (Construction Operating Model), Section 19
 * (Simulation Engine — required demo scenario).
 */
export const DEMO_PROJECT_ID = "proj_harbor_point";

// Fixed "current date" for the demo so seed data is deterministic.
// Tasks are scheduled relative to this so the scenario always makes sense.
export const DEMO_CURRENT_DATE = "2026-07-08T07:00:00.000Z";
